import React from 'react';

const Header = () => {
    return(
        <div className={"item1"}>
            <button id="logout" className={"btn"}>logout</button>
        </div>
    );
}

export default Header;